import SwiftUI
import WKView

//Used a package instead - import WKViEw


struct SelectServices: View {
    init(){
           UITableView.appearance().backgroundColor = .clear
       }
    
    @State var verified: Bool = false
    
    @State var password = ""
    var body: some View {
        VStack{
            Spacer()
            Text("Sign up for KidVest")
                .bold()
                .lineLimit(0)
                .font(.title2)
                .padding()
            
            Form{
                Section {
                    TextField("Enter your phone number", text: $password)
                        .padding()
                        .background(Color(.secondarySystemBackground))
                        .cornerRadius(8)
                        .shadow(color: .secondary, radius: 2, x: 1, y: 1)
                }
                Section (footer: Text("Your password must be 8 characters or more")){
                    TextField("Create a password", text: $password)
                        .padding()
                        .background(Color(.secondarySystemBackground))
                        .cornerRadius(8)
                        .shadow(color: .secondary, radius: 2, x: 1, y: 1)
                }
                Button(action: {
                    
                    
                }, label: {Text("Verify phone number")})
                    .frame(width: 200, height: 50)
                    .foregroundColor(.black)
                    .background(Color.white)
                    .cornerRadius(8)
                    .shadow(color: .secondary, radius: 2, x: 1, y: 1)
               
            }
            
            Button(action: {
                
                
            }, label: {Text("Create account")})
                .frame(width: 200, height: 50)
                .foregroundColor(.white)
                .background(Color.blue)
                .cornerRadius(8)
                .shadow(color: .secondary, radius: 2, x: 1, y: 1)
            Spacer()
            Spacer()
            
        }
        .padding()
        .navigationBarHidden(true)
    }
}

struct SelectServices_Previews: PreviewProvider {
    static var previews: some View {
        SelectServices()
    }
}
